name: app_custom_lints
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/AlexxyQQ/custom_lints

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  custom_lint_builder: ^0.7.5
  analyzer: ^7.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  very_good_analysis:
