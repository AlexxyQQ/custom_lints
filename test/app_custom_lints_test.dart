import 'package:test/test.dart';

void main() {
  group('app_custom_lints', () {
    test('placeholder test', () {
      // TODO: Write actual analyzer tests using test_reflective_loader or similar.
      // Currently, testing via the `example/` project is recommended.
      expect(true, isTrue);
    });
  });
}
